const getters = {
  isTongji: (state) => state.user.isTongji,
  // TimReady: state => state.message.TimReady,
  // onMessageReceived: state => state.message.onMessageReceived,
  // onMessageReceivedNumber: state => state.message.onMessageReceivedNumber
}
export default getters
